import { syncReadFile } from "../utils.js";

let lines = syncReadFile(`./input.txt`, "\n\n");
// sample.txt expects 11687500 as answer for part 1
// sample.txt expects 167409079868000 as answer for part 2

// Parse the input
